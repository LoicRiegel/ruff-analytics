[project]
name = "ruff-analytics"
version = "0.1.0"
description = "Scripts to gather and analyze ruff usage data"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
]

[tool.uv.workspace]
members = [
    "tmp",
]
